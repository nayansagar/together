<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:facebook="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#1e8eb8"
    tools:context="together.com.homely.LoginActivity">

    <!--<com.facebook.widget.LoginButton
        android:id="@+id/fb_login_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        facebook:confirm_logout="false"
        facebook:fetch_user_info="true" />-->

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/login_splash"
        android:src="@drawable/blocked_eclipse_dkblue_splash"
        android:layout_marginTop="100dp"/>

    <!--<ImageButton
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/login_facebook_button"
        android:onClick="onFacebookLoginButtonPress"
        android:layout_below="@id/login_splash"
        android:layout_marginTop="20dp"
        />-->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/login_with_facebook"
        android:background="@drawable/button_background_rounded"
        android:textColor="#1e8eb8"
        android:onClick="onFacebookLoginButtonPress"
        android:layout_below="@id/login_splash"
        android:layout_marginTop="50dp"
        android:layout_centerInParent="true"/>

    <!--<TextView
        android:id="@+id/username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="10dp"
        android:layout_marginTop="25dp"
        android:textSize="16sp"
        android:layout_below="@id/fb_login_button"/>-->

</RelativeLayout>
